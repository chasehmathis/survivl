% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deterministic.R
\name{dose_escalation}
\alias{dose_escalation}
\alias{is_deterministic}
\title{Deterministic treatment-assignment rules}
\usage{
dose_escalation(
  max_level,
  side_effect,
  init_prob = 0.5,
  trigger = function(z) z > 0,
  slip = 0
)

is_deterministic(x)
}
\arguments{
\item{max_level}{highest dose level \code{L} (levels run \verb{0, 1, ..., max_level})}

\item{side_effect}{name (stem) of the time-varying side-effect variable
(e.g. \code{"Z"}); the column \code{paste0(side_effect, "_", k)} is read at time \code{k}}

\item{init_prob}{probability the time-0 coin flip lands on the lowest active
dose (\code{1}) rather than placebo (\code{0})}

\item{trigger}{function mapping the side-effect variable to a logical vector
that is \code{TRUE} where a bad side effect occurred (default treats any
positive value as a side effect)}

\item{slip}{reserved for future use; must be \code{0} (fully deterministic)}

\item{x}{an object to test}
}
\value{
An object of class \code{c("causl_deterministic", "causl_family")}.
}
\description{
Helpers for specifying a treatment that is a \emph{deterministic} function of the
past, rather than a draw from a GLM.  The returned object is dropped into the
treatment slot of the \code{family} list passed to \code{\link[=msm_samp]{msm_samp()}} / \code{\link[=rmsm]{rmsm()}}, in the
same position where one would otherwise put a numeric family code (e.g. \code{5}
for binomial).
}
\details{
\code{\link[=dose_escalation]{dose_escalation()}} encodes a dose-escalation regime over ordered dose
levels \code{0} (placebo), \code{1} (lowest active dose), ..., \code{max_level} (highest):
\itemize{
\item \strong{time 0:} the dose is a coin flip between placebo (\code{0}) and the lowest
active dose (\code{1}), with \code{P(dose = 1) = init_prob};
\item \strong{no side effect} (\code{trigger(Z_k)} is \code{FALSE}): escalate one level, capped
at \code{max_level}, i.e. \verb{A_k = min(A_\{k-1\} + 1, max_level)};
\item \strong{bad side effect} (\code{trigger(Z_k)} is \code{TRUE}): de-escalate one level,
floored at placebo, i.e. \verb{A_k = max(A_\{k-1\} - 1, 0)}.
}

At the top level the same rule applies: with no side effect the patient
stays at \code{max_level}, and a side effect knocks them down a level.

The rule is evaluated inside the per-time-step simulation block \emph{after} the
time-varying covariates (including the side-effect variable) have been drawn,
so \code{Z_k} is available when \code{A_k} is assigned.  The treatment formula in
\code{formulas} is only used to register the treatment variable name and its place
in the variable ordering; the right-hand side is ignored (use \code{A ~ 1}).  No
\code{beta}/parameter entry is required for a deterministic treatment.

Currently the regime is fully deterministic given the past and the
side-effect process (the only stochastic input is the time-0 coin flip), so
the conditional treatment "propensity" is degenerate (0/1).  The \code{slip}
argument is reserved for a future extension that adds a small per-transition
slip probability, restoring positivity for inverse-probability weighting.
}
\section{Functions}{
\itemize{
\item \code{is_deterministic()}: Test whether a family entry is a deterministic rule

}}
\examples{
\dontrun{
forms <- list(list(B ~ 1),
              Z ~ B + A_l1,                    # side-effect covariate
              A ~ 1,                            # treatment: RHS ignored
              Y ~ B + I(A_0 + A_1 + A_2),
              Z ~ 1)
fams <- list(list(1),
             5,                                 # binary side effect
             dose_escalation(max_level = 3, side_effect = "Z"),
             1,
             1)
}

}
